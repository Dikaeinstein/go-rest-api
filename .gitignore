# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

.env
# Ignore vendor packages but include vendor.json to build vendor packages
vendor/**
!vendor/vendor.json
.vscode
# ignore app binary
go-rest-api
